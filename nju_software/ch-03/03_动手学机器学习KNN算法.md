# 动手学机器学习 KNN 算法

## 1. 学习目标

通过本章节的学习，学生应能够：

1. **理解** KNN 算法的核心思想（“物以类聚”）和“惰性学习”的特点。
2. **掌握** KNN 算法的三要素：距离度量、K 值选择、决策规则。
3. **应用** 欧氏距离公式进行手动计算，完成简单的分类任务（如电影题材分类）。
4. **实现** 基于 `scikit-learn` 的 KNN 分类流程，包括数据预处理（标准化）、模型训练与评估。
5. **分析** K 值大小对模型过拟合与欠拟合的影响，以及数据标准化的必要性。

---

## 2. 算法概述

**K 近邻**（`K-Nearest Neighbors`，`KNN`）是一种基本的机器学习算法，广泛应用于分类和回归任务。它的核心思想是：“**物以类聚**”，即如果一个数据点在特征空间中与某些已知类别的数据点靠得很近，那么它很可能属于这些数据点所属的类别。

在机器学习的广阔领域中，**K 近邻**算法以其独特的“**惰性学习**”特质脱颖而出。所谓惰性学习，即算法在训练阶段几乎不进行模型构建，而是将所有数据都“原样”保存，等到预测时才根据需要进行计算。这种策略虽看似“偷懒”，却在实际应用中大放异彩。

从电影推荐系统到手写数字识别，`KNN` 算法都展现出其强大的通用性。例如：

- 在电影推荐场景中，通过分析与目标用户兴趣相似的 `K` 个邻居的观影喜好，为用户精准筛选出潜在喜爱的电影；
- 在手写数字识别任务里，基于数字图像的特征，找出训练集中最相近的 `K` 个样本，从而判断新数字的类别。

其核心特点在于无需复杂的训练过程，逻辑直观易懂，即使初学者也能快速上手，为各类数据挖掘任务提供有效解决方案。

---

## 3. 算法原理

### 3.1 核心思想

`KNN` 算法的基本假设是，具有相似特征的对象在空间中彼此靠近。简单来说，一个未知类别的样本，其“**邻居**”的类别能在很大程度上反映出自身所属类别。在数学上，分类任务常用“多数表决”的形式表达为：

$$
\hat{y}(x) = \arg\max_{c_j} \sum_{x_i \in N_k(x)} \mathbb{I}(y_i = c_j)
$$

其中，$N_k(x)$ 表示样本 $x$ 的 $K$ 个最近邻样本集合，$\mathbb{I}(y_i = c_j)$ 是指示函数，若邻居样本 $x_i$ 的类别 $y_i$ 属于类别 $c_j$ 则取 1，否则取 0。通俗地讲，就是统计 $K$ 个邻居中各类别出现的次数，将出现次数最多的类别作为样本 $x$ 的预测类别。

### 3.2 关键三要素

- **距离度量**：距离度量方式直接决定了样本之间“相似程度”的量化。常见的有欧氏距离、曼哈顿距离和余弦相似度，具体对比如下：

  | **度量方式**   | **公式**                                          | **适用场景** |
  | :------------- | :------------------------------------------------ | :----------- |
  | **欧氏距离**   | $\sqrt{\sum_{i=1}^n (x_i-y_i)^2}$                 | 连续特征     |
  | **曼哈顿距离** | $\sum_{i=1}^n \lvert x_i-y_i\rvert$               | 网格路径     |
  | **余弦相似度** | $\frac{X \cdot Y}{\lVert X\rVert \lVert Y\rVert}$ | 文本数据     |

  **欧氏距离**衡量的是两点在 **n** 维空间中的直线距离，适用于具有连续数值特征的数据；**曼哈顿距离**则是在网格状路径中计算两点之间的绝对距离之和，常用于城市街区等类似场景；**余弦相似度**重点关注两个向量在方向上的相似性，文本数据因高频词汇的特性，使用余弦相似度能更好地反映文本间语义关联。

- **K 值选择**：`K` 值的选取对模型性能至关重要。较小的 `K` 值（如 K=1）相当于只相信最近的一个邻居，模型复杂度高，容易受噪声点影响导致**过拟合**（Overfitting）；较大的 `K` 值则相当于参考大范围内的邻居，模型过于平滑，可能忽略局部特征导致**欠拟合**（Underfitting）。

  在实际应用中，通常采用**交叉验证**（Cross Validation）的方法来确定最佳 `K` 值。即在验证集上尝试不同的 `K` 值，绘制“准确率 - K 值”曲线，选择准确率最高且模型较简单的 `K` 值。同时，为避免在二分类中出现平票情况，`K` 值通常选择奇数。

- **决策规则**：在分类任务中，多数表决法是最常用的决策规则。即统计 `K` 个邻居中每个类别出现的次数，将出现次数最多的类别作为预测类别；也可引入权重机制，依据邻居到样本点的距离给予不同投票权重，距离近的邻居权重更高。在回归任务中，预测值为 `K` 个邻居目标值的均值或加权均值，权重同样可基于距离设置。

### 3.3 实例推导：电影题材分类

为了让大家更直观地理解 `KNN` 的计算过程，我们使用一个经典的**电影题材分类**案例进行手动推导。

**场景描述**：
我们统计了 4 部已知电影的“打斗镜头次数”和“接吻镜头次数”，并已知它们是“动作片”还是“爱情片”。现在有一部新电影，已知其镜头数据，需要预测它的类型。

**数据集**：

|   电影名称   | 打斗镜头次数 ($x_1$) | 接吻镜头次数 ($x_2$) | 电影类型 ($y$) |
| :----------: | :------------------: | :------------------: | :------------: |
|      A       |          2           |          10          |     爱情片     |
|      B       |          3           |          11          |     爱情片     |
|      C       |          12          |          4           |     动作片     |
|      D       |          11          |          2           |     动作片     |
| **新电影 E** |        **10**        |        **5**         |     **?**      |

**设定参数**：

- $K = 3$（选择最近的 3 个邻居）
- 距离度量：**欧氏距离**

**步骤 1：计算距离**：

计算**新电影 E (10, 5)** 与所有已知电影的欧氏距离：

1. **E - A(2, 10)**: $\sqrt{(10-2)^2 + (5-10)^2} = \sqrt{64 + 25} = \sqrt{89} \approx 9.43$
2. **E - B(3, 11)**: $\sqrt{(10-3)^2 + (5-11)^2} = \sqrt{49 + 36} = \sqrt{85} \approx 9.22$
3. **E - C(12, 4)**: $\sqrt{(10-12)^2 + (5-4)^2} = \sqrt{4 + 1} = \sqrt{5} \approx 2.24$
4. **E - D(11, 2)**: $\sqrt{(10-11)^2 + (5-2)^2} = \sqrt{1 + 9} = \sqrt{10} \approx 3.16$

**步骤 2：排序与筛选**：

将距离从小到大排序，并找出前 $K=3$ 个最近邻：

| 排名 | 电影名称 | 距离 |  类型  |
| :--: | :------: | :--: | :----: |
|  1   |  **C**   | 2.24 | 动作片 |
|  2   |  **D**   | 3.16 | 动作片 |
|  3   |  **B**   | 9.22 | 爱情片 |
|  4   |    A     | 9.43 | 爱情片 |

**步骤 3：多数表决**：

统计 $K=3$ 个最近邻（C, D, B）的类别：

- **动作片**（来自 C, D）：2 票
- **爱情片**（来自 B）：1 票

**结论**：根据多数表决规则，预测**新电影 E** 为 **动作片**。

### 3.4 算法复杂度分析

`KNN` 算法在**训练阶段几乎没有计算开销**，主要计算成本集中在**预测阶段**。对于一个包含 $n$ 个样本，每个样本具有 $d$ 维特征的数据集，预测时的计算复杂度如下：

1. **计算距离**：
   需要计算待预测样本与所有训练样本的距离，时间复杂度为：

   $$
   O(n d)
   $$

2. **筛选 K 个最近邻**：
   采用排序或堆结构进行筛选，时间复杂度为：
   - **直接排序**：$O(n \log n)$（不推荐）
   - **维护小顶堆**：$O(n \log K)$（常见优化方案）
   - **朴素筛选（逐次选取最小）**：$O(nK)$
   - **选择算法（Quickselect，期望）**：$O(n)$（得到第 $K$ 小距离阈值后再线性扫描）

因此，整体时间复杂度约为：

$$
O(n d + n \log K)
$$

在实际应用中，如果数据规模庞大（例如百万级样本），`KNN` 的计算开销较大，因此常用 **KD-Tree**、**Ball-Tree** 或 **近似最近邻搜索（ANN）** 来优化查询效率。

**空间复杂度**主要由存储所有训练样本的数据决定，大小约为：

$$
O(n d)
$$

这意味着数据量较大时，存储成本也是一个需要考虑的因素。

---

## 4. 算法优缺点

### 4.1 优点

1. **简单易懂**：原理简单，无需复杂的数学推导，易于实现和解释。
2. **无显式训练**：作为惰性学习算法，训练阶段主要是存储数据；预测阶段再计算距离并进行决策。
3. **适用性广**：既可用于分类，也可用于回归；对噪声与异常值的敏感性与 $K$、距离度量和数据预处理相关。

### 4.2 缺点

1. **计算量大**：预测时需要计算与所有训练样本的距离，计算复杂度高，不适合大规模数据集。
2. **内存消耗大**：需要存储所有训练数据，空间复杂度高。
3. **样本不平衡敏感**：当样本不平衡时，预测结果容易偏向大类样本。

---

## 5. 经典案例实战：鸢尾花分类

### 5.1 环境准备

要运行 `KNN` 算法进行鸢尾花分类，需先准备好相应的 `Python` 环境与库。以下为必需的库导入代码：

```python
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import pandas as pd
from sklearn.datasets import load_iris
from sklearn.neighbors import KNeighborsClassifier
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import classification_report, confusion_matrix

# 设置绘图风格
sns.set(style="whitegrid")
```

### 5.2 数据探索与预处理

加载鸢尾花数据集，并对其进行探索性分析（EDA）和预处理。

**1. 数据可视化**：

在进行模型训练之前，先通过散点图矩阵观察特征之间的关系，这有助于我们直观理解数据的可分性。

```python
# 加载数据并创建 DF
iris = load_iris()
X = iris.data
y = iris.target
feature_names = iris.feature_names
target_names = iris.target_names

# 可视化数据分布
df = pd.DataFrame(X, columns=feature_names)
df['species'] = y
df['species'] = df['species'].map({0: 'setosa', 1: 'versicolor', 2: 'virginica'})

sns.pairplot(df, hue='species', markers=["o", "s", "D"])
plt.suptitle("鸢尾花数据集散点图矩阵", y=1.02)
plt.show()
```

通过 Pairplot 可以看到，Setosa 类别在多个特征维度上都与其他两个类别区分明显，而 Versicolor 和 Virginica 则有部分重叠。

**2. 数据集划分与标准化**：

```python
# 为了后续可视化决策边界方便，这里我们重新选用前两个特征
X_vis = iris.data[:, :2]
# 注意：在实际项目中通常使用所有特征进行训练，这里为了展示二维边界做了简化

# 划分训练集和测试集
# stratify=y 确保训练集和测试集中各类别的比例与原始数据集一致
X_train, X_test, y_train, y_test = train_test_split(
    X_vis, y, test_size=0.3, random_state=42, stratify=y
)

# 标准化特征数据，消除量纲影响
scaler = StandardScaler().fit(X_train)
X_train = scaler.transform(X_train)
X_test = scaler.transform(X_test)
```

此处选取了鸢尾花数据集的前两个特征，便于后续可视化展示。采用标准差标准化对数据进行处理，消除不同特征量纲对距离计算的影响，确保模型性能的稳定性。

### 5.3 模型训练与评估

通过循环遍历不同的 `K` 值，寻找最优的 `K` 值以构建最佳模型，并输出详细的评估报告。

```python
# 动态 K 值选择演示
best_k = 0
best_score = 0
scores = []

# 遍历 1 到 20 的 K 值
for k in range(1, 20):
    knn = KNeighborsClassifier(n_neighbors=k)
    knn.fit(X_train, y_train)
    score = knn.score(X_test, y_test)
    scores.append(score)
    if score > best_score:
        best_score = score
        best_k = k

print(f'Optimal K: {best_k} (Accuracy: {best_score:.2%})')

# 绘制 K 值与准确率的关系图
plt.figure(figsize=(10, 6))
plt.plot(range(1, 20), scores, marker='o')
plt.title('Accuracy vs. K Value')
plt.xlabel('K Value')
plt.ylabel('Accuracy')
plt.show()

# 使用最优 K 值重新训练模型并输出详细报告
knn_best = KNeighborsClassifier(n_neighbors=best_k)
knn_best.fit(X_train, y_train)
y_pred = knn_best.predict(X_test)

print("\n分类报告:")
print(classification_report(y_test, y_pred, target_names=target_names))

print("\n混淆矩阵:")
print(confusion_matrix(y_test, y_pred))
```

### 5.4 结果指标解读

通过上述代码，我们不仅得到了模型的准确率，还输出了分类报告（Classification Report）和混淆矩阵（Confusion Matrix）。以下是对这些指标的详细解读（以 K=3 为例）：

1. **混淆矩阵 (Confusion Matrix)**：

   - 矩阵的行表示真实类别，列表示预测类别。
   - 对角线上的数字表示被正确分类的样本数。
   - 例如，如果 `versicolor` 行 `virginica` 列有数字，说明真实的 `versicolor` 被误判为了 `virginica`。

2. **精确率 (Precision)**：

   - 针对预测结果而言，表示“预测为正类的样本中，有多少是真正的正类”。
   - 例如 `setosa` 的 Precision 为 1.00，意味着所有被模型预测为 `setosa` 的样本，确实都是 `setosa`。

3. **召回率 (Recall)**：

   - 针对真实样本而言，表示“所有真正的正类样本中，有多少被模型找出来了”。
   - 例如 `versicolor` 的 Recall 如果较低，说明有很多 `versicolor` 被漏检了（被误判为其他类别）。

4. **F1-score**：
   - 精确率和召回率的调和平均数，综合衡量模型的性能。

### 5.5 决策边界可视化（二维展示）

为了直观地呈现 `KNN` 模型的决策边界，利用以下代码绘制图形：

```python
# 使用最优 K 值重新训练模型
knn_best = KNeighborsClassifier(n_neighbors=best_k)
knn_best.fit(X_train, y_train)

# 创建网格数据
x_min, x_max = X_train[:, 0].min() - 1, X_train[:, 0].max() + 1
y_min, y_max = X_train[:, 1].min() - 1, X_train[:, 1].max() + 1
xx, yy = np.meshgrid(np.linspace(x_min, x_max, 200), np.linspace(y_min, y_max, 200))
Z = knn_best.predict(np.c_[xx.ravel(), yy.ravel()]).reshape(xx.shape)

# 绘制图形
plt.figure(figsize=(10, 6))
plt.contourf(xx, yy, Z, alpha=0.4, cmap='viridis')
sns.scatterplot(x=X_train[:, 0], y=X_train[:, 1], hue=iris.target_names[y_train],
                palette='Dark2', edgecolor='black', s=80)
plt.title(f"KNN Decision Boundaries (k={best_k})")
plt.xlabel(iris.feature_names[0])
plt.ylabel(iris.feature_names[1])
plt.legend(title='Species')
plt.show()
```

这段代码生成了一个二维网格，计算网格中每个点的预测类别，绘制出决策边界，并将训练数据点按类别着色绘制其上，清晰地展示了不同类别之间的划分区域。

---

## 6. 算法优化策略

### 6.1 维度灾难解决方案

随着特征维度的增加，数据在高维空间中变得稀疏，计算距离时会受到大量无关特征的干扰，导致“维度灾难”。为此，可采用 `PCA` 降维，保留数据中 **90%** 的方差，降低特征维度；或利用互信息法进行特征选择，筛选出与目标变量相关性最高的特征，减轻维度对 `KNN` 算法性能的影响。

### 6.2 效率提升方案

当数据量较大时，为提升 `KNN` 算法的查询效率，可以采用 `KD-Tree` 数据结构。一般而言，在**低维**场景下，`KD-Tree` 的查询复杂度通常可近似为 $O(\log n)$（最坏情况可能退化到 $O(n)$），并且构建树的复杂度常见为 $O(n \log n)$。此外，也可使用近似最近邻（`LSH` 哈希）方法，在牺牲少量精度的前提下，大幅提升大规模数据的查询速度。

### 6.3 样本不平衡处理

在实际数据中，若不同类别样本数量差异悬殊，普通 `KNN` 算法的预测结果会偏向样本数量多的类别。此时，可通过加权投票的方式，赋予距离近的邻居更高的权重；或采用 `SMOTE` 过采样技术，合成少数类样本，平衡数据集类别分布，提升模型对少数类的识别能力。

---

## 7. 思考与扩展讨论

为了加深对 KNN 算法的理解，建议同学们思考以下问题并进行尝试：

1. **K 值的极端情况**：
   - 如果 K = 1，模型会发生什么？
   - 如果 K = N（样本总数），模型会发生什么？
2. **特征量纲的影响**：
   - 尝试去掉数据预处理中的 `StandardScaler` 步骤，重新运行模型。观察准确率和决策边界的变化，体会标准化对基于距离的算法的重要性。
3. **不同距离度量**：
   - `KNeighborsClassifier` 默认使用欧氏距离（`minkowski`，`p = 2`）。尝试将其改为曼哈顿距离（`p = 1`），观察结果是否有变化。

---

## 8. 总结

`KNN` 算法凭借其简单直观的特点，在小规模数据、对解释性要求较高的场景下有着广泛的应用，如客户分类、模式识别等。然而，它也并非完美无缺，面对大数据和高维数据时存在一定的局限性。未来，随着深度学习技术的发展，将 `KNN` 算法与深度特征提取相结合，有望进一步提升其性能。对于学习者而言，从深入研究 `KD-Tree` 的源码实现入手，是理解 `KNN` 算法本质、掌握优化技巧的有效路径，这有助于更好地运用和改进 `KNN` 算法，使其在更广泛的领域中发挥作用。

---

## 9. 参考文献

1. Cover, T., & Hart, P. (1967). Nearest neighbor pattern classification. _IEEE transactions on information theory_, 13(1), 21-27.
2. Pedregosa, F., et al. (2011). Scikit-learn: Machine Learning in Python. _Journal of Machine Learning Research_, 12, 2825-2830.

---

## 10. 练习题参考答案

1. **K 值的极端情况**：
   - **K=1**：模型只依赖最近的一个邻居，决策边界会非常曲折、对噪声点敏感，训练集表现往往很好但泛化能力较差，属于典型的过拟合风险场景。
   - **K=N**：每次预测都等价于对全体样本做多数表决，输出会趋向于训练集中样本数最多的类别，模型几乎失去区分能力，属于欠拟合或“退化”情形。
2. **特征量纲的影响**：
   - 去掉 `StandardScaler` 后，距离计算会被数值范围更大的特征主导，导致“相似度”度量失真，从而影响近邻选择、准确率以及决策边界形状。标准化可让不同量纲的特征在距离计算中贡献更均衡。
3. **不同距离度量**：
   - 欧氏距离（\(p=2\)）对单个维度的大偏差更敏感；曼哈顿距离（\(p=1\)）对各维度差异做线性累加，几何上等距曲线从“圆形”变为“菱形”。在特征分布或异常值结构不同的情况下，两者的近邻集合可能不同，从而导致分类边界与准确率发生变化；若数据本身可分性较强，变化可能较小。
